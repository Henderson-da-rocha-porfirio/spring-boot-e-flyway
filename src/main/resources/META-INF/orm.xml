<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings
        xmlns="http://xmlns.jcp.org/xml/ns/persistence/orm"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/orm_2_2.xsd"
        version="2.2">

    <!--    1. named-query: é a query nomeada.
            2. Em name, é necessário colocar o mesmo nome do método passado no repository correspondente
            3. <query>: é uma consulta do nosso JPQL -->
    <named-query name="Restaurant.consultarPorNome">
        <query>
            from Restaurant
            where nome like concat('%', :nome, '%')
            and kitchen.id = :id
        </query>
    </named-query>

</entity-mappings>